指定フォルダ以下の指定拡張子の容量を確認するマクロになります。
拡張子指定をして実行することが多かったので、今回はPSDに固定して
容量を計算しております。

実機へ出力する際はDDSファイルなどの拡張子に変更してお使い下さい。
変更方法は、スクリプトの14行目の
【if (objFSO.GetExtensionName(objItem) == "psd")】
のpsdの部分を変更するだけでOKです。
